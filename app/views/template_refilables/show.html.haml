.container-fluid.container-fixed-lg
  .panel.panel-default
    .panel-body
      %h2
        = @template_refilable.name
      %p
        = @template_refilable.description

      .well
        = raw remove_class(replace_fillable(@template_refilable.content))
