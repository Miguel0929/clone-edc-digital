= bootstrap_form_for(@routecover) do |f|
	= f.text_field :name, label: 'Nombre de la imagen:'
	= f.file_field :route_image, label: 'Elige la imagen de Mi ruta'
	.pull-right
		=link_to dashboard_pathway_path do
			%button.btn.btn-info.m-l-10
				%i.fa.fa-times
				Cancelar
		= f.submit 'Subir imagen', class: 'btn btn-success m-l-10'