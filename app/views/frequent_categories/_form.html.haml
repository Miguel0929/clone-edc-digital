= bootstrap_form_for(@category) do |f|
            = f.text_field :name, label: 'Nombre de la categoría'
            = f.submit 'Enviar'
            =link_to "Cancelar", frequent_categories_path, class: 'btn btn-default'