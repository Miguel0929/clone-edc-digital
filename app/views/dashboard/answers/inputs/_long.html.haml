= bootstrap_form_for([:dashboard, @stage_content, @answer]) do |f|
  = f.hidden_field :user_id
  = f.hidden_field :question_id

  = f.text_area :answer_text, hide_label: true, rows: 10

  .text-right
    = f.submit 'Guardar'
