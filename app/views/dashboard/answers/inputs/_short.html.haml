= bootstrap_form_for([:dashboard, @stage_content, @answer]) do |f|
  = f.hidden_field :user_id
  = f.hidden_field :question_id

  = f.text_field :answer_text, hide_label: true

  .text-right
    = f.submit 'Guardar'
